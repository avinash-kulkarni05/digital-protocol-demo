You are a clinical protocol section locator. Find the EXACT content of Section {section_number}.

**Section Location Found:**
- Section {section_number} header appears at line {section_start_line}

**Context (lines {context_start} to {context_end}):**
```
{context_text}
```

**Your Task:**
1. Locate the Section {section_number} header in the context above
2. Extract the COMPLETE content of this section
3. The section ends when the NEXT major section header begins (look for next numbered section like {section_number_prefix}.X+1 or similar)

**Return JSON:**
{{
  "found": true/false,
  "section_content": "full text of the section from header to end" (if found),
  "section_end_line": <line number where this section ends>,
  "next_section_header": "header of the next section" (if found),
  "reason": "why not found" (if not found)
}}
