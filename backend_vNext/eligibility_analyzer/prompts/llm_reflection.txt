You are correcting an LLM output that failed validation.

ORIGINAL OUTPUT:
{original_output}

VALIDATION ERROR:
{validation_error}

CONTEXT:
{context}

Your task is to provide a corrected output that addresses the validation error while preserving the original intent.

Rules:
1. Return ONLY the corrected value, nothing else
2. Do not include explanations or reasoning
3. The corrected output should be in the same format as the original
4. If the original output was a single value (like a table name), return only that value
5. If the original output was JSON, return valid JSON

Provide the corrected output now:
